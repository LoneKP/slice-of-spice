<div class="text-5xl lg:text-base bg-yellow h-12 flex justify-between items-center mb-8 border-peach border-solid border-b-2 px-4">
  <%= render "shared/nav" %>
  <div class="flex flex-col justify-center max-w-prose">
    <% if user_signed_in? %>
        <%= render "shared/user_signed_in" %>
    <% else %>
        <%= render "shared/user_not_signed_in" %>
    <% end %>
  </div>
</div>