<div class="flex flex-row items-center gap-2">
  <p><%= "#{current_user.name}" %></p>
  <%= button_to destroy_user_session_path, method: :delete do %>
    <button class="w-24 py-1">Sign out</button>
  <% end %>
</div>