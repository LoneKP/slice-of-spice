<!-- inside your mobile menu list -->
<ul class="space-y-8 text-2xl text-center text-almostblack font-semibold">
    <li><%= link_to "Mealplanr",        recipes_path,          class: "hover:underline" %></li>
</ul>
<% if user_signed_in? %>
<ul class="space-y-8 text-2xl text-center text-almostblack">
    <li><%= link_to "My recipes",       my_recipes_path,       class: "hover:underline" %></li>
    <li><%= button_to 'Sign out', destroy_user_session_path, method: :delete,
    class: 'block w-full text-left px-4 py-2 text-xl lg:hidden' %></li>
</ul>
 <% else %>
 <%= render "shared/user_not_signed_in" %>
 <% end %>
        

 
